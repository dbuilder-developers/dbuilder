[ProjectName]
    type        = shared
    jobs        = 4
    prefix      = /usr
    bindir      = bin
    datadir     = data
    docdir      = doc
    includedir  = include/d
    libdir      = lib64
    pkgconfigdir= pkgconfig
    importsdir  = /usr/include/d,/usr/local/include/d
    linktolib   = derelict
    sourcedir   =
    builddir    =
